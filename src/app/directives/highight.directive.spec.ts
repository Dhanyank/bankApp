import { HighightDirective } from './highight.directive';

describe('HighightDirective', () => {
  it('should create an instance', () => {
    const directive = new HighightDirective();
    expect(directive).toBeTruthy();
  });
});
